# ENVIRONMENT
machine:
  ruby:
    version: 2.1.2

# DEPLOYMENT
deployment:
  prod:
   branch: master
   commands:
     - git push git@heroku.com:bemotion-web.git $CIRCLE_SHA1:refs/heads/master
     - heroku run rake db:migrate --app bemotion-web:
         timeout: 400
