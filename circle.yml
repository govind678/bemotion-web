# ENVIRONMENT
machine:
  ruby:
    version: 2.1.2

deployment:
  staging:
    branch: master
    heroku:
      appname: bemotion-web

  prod:
    branch: master
    heroku:
      appname: bemotion-web
      command:
        - heroku run rake db:migrate
